name: news_demo_bloc
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc:
  dio:
  equatable:
  firebase_core:
  firebase_auth:
  google_sign_in:
  hive:
  path_provider:
  url_launcher:
  webview_flutter:
  share:
  turkish:
  json_annotation:


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  json_serializable:

flutter:
  uses-material-design: true
